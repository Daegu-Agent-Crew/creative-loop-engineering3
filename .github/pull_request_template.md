## Summary

-

## CLE3 Checks

- [ ] I checked `AGENTS.md` for the relevant phase.
- [ ] For panel work, I ran or reviewed `node scripts/run-panel-jobs.js --episode EPxxx --dry-run --max-jobs 3`.
- [ ] For panel assets, I followed `docs/ASSET-STORAGE-POLICY.md`.
- [ ] I ran `node scripts/check-panel-assets.js` when adding or changing panel assets.
- [ ] I did not commit original, candidate, rejected, or base image files.

## Validation

- 실행 명령 / 실제 결과 / 로그 또는 CI 링크:
- 검사한 commit SHA:
- 산출물·asset 경고:

## Merge decision (별도 사람 결정)

- 결정 담당자:
- 요청하는 결정 한 가지:
- 대기 시작 시각:
- 결정 기한 / 기한 초과 시 처리:
- `operations/decision-queue.json` 항목 ID:

## Release (작업 병합과 별도)

- 대상 episode / phase:
- 원본 Release Approval 상태 / 증거 경로:
- 남은 QA·오버레이·페이지 매핑 문제:

CI 성공이나 체크박스는 사람 승인·발행 완료의 근거가 아니다.
`node scripts/decision-status.js --github` 결과로 검증·병합·공개 상태를 분리해서 보고한다.
